name: app-yaml-env-compiler

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:

  build:
    name: Test
    runs-on: ubuntu-latest
    steps:

    - name: Test compiler
      uses: ikuanyshbekov/app-yaml-env-compiler@v1
